# Loaded only for `vite build --mode e2e` + Playwright (see playwright.config.ts).
# Forces the desktop “visible add-to-cart on homepage cards” treatment without LaunchDarkly.
VITE_PLAYWRIGHT_ATC_TREATMENT=true
