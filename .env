# Committed defaults for local dev (safe to commit).
# LaunchDarkly client-side IDs are public by design — they're embedded in the
# browser bundle. Do NOT put secrets (e.g. LAUNCHDARKLY_API_ACCESS_TOKEN) here;
# keep those in the untracked .env.local.
#
# This points at the LaunchDarkly "test" environment for the flagship-store project.
VITE_LD_CLIENT_ID=69e2a56f4dc6f50a9c600aec
