# Experiment Hunter — developer handoff

This file was generated automatically when **Test Idea** opened this pull request. If GitHub Actions or Playwright fails, share the sections below with your team or paste the prompt into your coding agent.

## Links

- **Flag (LaunchDarkly):** `eh-desktop-promo-code-copy-desktop`
- **Experiment (LaunchDarkly):** `eh-desktop-promo-code-copy-desktop-exp`

## Summary

### Title
Make the top promo code copyable

### Hypothesis
If we change the top banner promo code from static text into a copyable pill button with feedback, then promo code interaction rate and cart progression will improve, because saving the offer reduces the need to memorize or retype the code later in checkout.

### Arms
- **Control:** The top banner displays the code as static text.
- **Treatment:** The code appears as a pill button with 'Copy code' feedback and optional confirmation toast.

### Metrics
Promo code interaction rate; Cart icon clicks; Checkout starts; Orders using promo code

## Copy-paste prompt (for your dev / AI assistant)

```text
You are helping fix or finish a code change for a storefront repo opened by Experiment Hunter (A/B test wiring).

**LaunchDarkly flag key:** `eh-desktop-promo-code-copy-desktop`
**LaunchDarkly experiment key:** `eh-desktop-promo-code-copy-desktop-exp`

**Experiment title:** Make the top promo code copyable
**Hypothesis:** If we change the top banner promo code from static text into a copyable pill button with feedback, then promo code interaction rate and cart progression will improve, because saving the offer reduces the need to memorize or retype the code later in checkout.

**Control (current):** The top banner displays the code as static text.
**Treatment (variant):** The code appears as a pill button with 'Copy code' feedback and optional confirmation toast.

**Metrics to preserve or instrument:** Promo code interaction rate; Cart icon clicks; Checkout starts; Orders using promo code

**Requirements:**
- Implement the treatment so it is correctly gated by LaunchDarkly flag `eh-desktop-promo-code-copy-desktop` (read via the project’s existing LaunchDarkly client / hook pattern).
- Keep TypeScript and ESLint clean; match existing file structure and naming.
- If the repo uses Playwright (or another E2E runner), add or update tests so CI can prove the treatment behavior when the LaunchDarkly flag is **on** in the test environment (or document a test-only flag override if the team uses that pattern).
- If CI failed, read the GitHub Actions logs for this PR, fix the root cause, and push commits to the same branch.
```

_Generated by Experiment Hunter. Safe to delete after merge if you prefer not to keep it in the repo._
