# Experiment Hunter — developer handoff

This file was generated automatically when **Test Idea** opened this pull request. If GitHub Actions or Playwright fails, share the sections below with your team or paste the prompt into your coding agent.

## Links

- **Flag (LaunchDarkly):** `eh-desk-always-visible-atc-desktop`
- **Experiment (LaunchDarkly):** `eh-desk-always-visible-atc-desktop-exp`

## Summary

### Title
Always-Visible Add to Cart Buttons on Product Cards (briannorman

### Hypothesis
If product card Add to Cart buttons are always visible instead of hover-only, then add-to-cart rate from the homepage will increase, because persistent action buttons reduce interaction cost and signal purchasability at a glance.

### Arms
- **Control:** Add to Cart button is hidden and only appears on mouse hover over the product card.
- **Treatment:** Add to Cart button is always visible at the bottom of every product card without requiring hover.

### Metrics
FlagShip — Add to cart — FlagShip — Add to cart; Best Seller card click rate: Overall click rate on Best Sellers product cards (image; title; or ATC) — tracked via best_seller_card_click event.; Revenue per visitor (if available): Average revenue per homepage visitor to detect whether increased ATC translates to downstream revenue. TBD based on data availability.; Total add-to-cart actions originating from homepage product cards (Best Sellers + New Arrivals). Measured as a conversion rate: unique visitors who add at least one item to cart from the homepage / total unique desktop homepage visitors in the experiment.; FlagShip — PDP view; FlagShip — Cart view

## Copy-paste prompt (for your dev / AI assistant)

```text
You are helping fix or finish a code change for a storefront repo opened by Experiment Hunter (A/B test wiring).

**LaunchDarkly flag key:** `eh-desk-always-visible-atc-desktop`
**LaunchDarkly experiment key:** `eh-desk-always-visible-atc-desktop-exp`

**Experiment title:** Always-Visible Add to Cart Buttons on Product Cards (briannorman
**Hypothesis:** If product card Add to Cart buttons are always visible instead of hover-only, then add-to-cart rate from the homepage will increase, because persistent action buttons reduce interaction cost and signal purchasability at a glance.

**Control (current):** Add to Cart button is hidden and only appears on mouse hover over the product card.
**Treatment (variant):** Add to Cart button is always visible at the bottom of every product card without requiring hover.

**Metrics to preserve or instrument:** FlagShip — Add to cart — FlagShip — Add to cart; Best Seller card click rate: Overall click rate on Best Sellers product cards (image; title; or ATC) — tracked via best_seller_card_click event.; Revenue per visitor (if available): Average revenue per homepage visitor to detect whether increased ATC translates to downstream revenue. TBD based on data availability.; Total add-to-cart actions originating from homepage product cards (Best Sellers + New Arrivals). Measured as a conversion rate: unique visitors who add at least one item to cart from the homepage / total unique desktop homepage visitors in the experiment.; FlagShip — PDP view; FlagShip — Cart view

**Requirements:**
- Implement the treatment so it is correctly gated by LaunchDarkly flag `eh-desk-always-visible-atc-desktop` (read via the project’s existing LaunchDarkly client / hook pattern).
- Keep TypeScript and ESLint clean; match existing file structure and naming.
- If the repo uses Playwright (or another E2E runner), add or update tests so CI can prove the treatment behavior when the LaunchDarkly flag is **on** in the test environment (or document a test-only flag override if the team uses that pattern).
- If CI failed, read the GitHub Actions logs for this PR, fix the root cause, and push commits to the same branch.
```

_Generated by Experiment Hunter. Safe to delete after merge if you prefer not to keep it in the repo._
