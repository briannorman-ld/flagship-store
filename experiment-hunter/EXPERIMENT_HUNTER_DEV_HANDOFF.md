# Experiment Hunter — developer handoff

This file was generated automatically when **Test Idea** opened this pull request. If GitHub Actions or Playwright fails, share the sections below with your team or paste the prompt into your coding agent.

## Links

- **Flag (LaunchDarkly):** `eh-desktop-nav-shop-all-destination-desktop`
- **Experiment (LaunchDarkly):** `eh-desktop-nav-shop-all-destination-desktop-exp`

## Summary

### Title
Route Browse All Categories to a true category hub

### Hypothesis
If we change the hero secondary CTA destination so that 'Browse All Categories' opens a category overview page or scrolls to the Shop by Category section with all category options visible, then category browsing click-through rate will improve, because the destination will match the shopper's promised browsing intent instead of sending them into a single category path.

### Arms
- **Control:** The hero secondary CTA is labeled 'Browse All Categories' but sends shoppers into a single category path.
- **Treatment:** The CTA opens a category overview page or scrolls to the Shop by Category section with all category options visible.

### Metrics
Category browsing click-through rate; Hero secondary CTA clicks; Category tile clicks after hero interaction; Bounce rate from CTA destination

## Copy-paste prompt (for your dev / AI assistant)

```text
You are helping fix or finish a code change for a storefront repo opened by Experiment Hunter (A/B test wiring).

**LaunchDarkly flag key:** `eh-desktop-nav-shop-all-destination-desktop`
**LaunchDarkly experiment key:** `eh-desktop-nav-shop-all-destination-desktop-exp`

**Experiment title:** Route Browse All Categories to a true category hub
**Hypothesis:** If we change the hero secondary CTA destination so that 'Browse All Categories' opens a category overview page or scrolls to the Shop by Category section with all category options visible, then category browsing click-through rate will improve, because the destination will match the shopper's promised browsing intent instead of sending them into a single category path.

**Control (current):** The hero secondary CTA is labeled 'Browse All Categories' but sends shoppers into a single category path.
**Treatment (variant):** The CTA opens a category overview page or scrolls to the Shop by Category section with all category options visible.

**Metrics to preserve or instrument:** Category browsing click-through rate; Hero secondary CTA clicks; Category tile clicks after hero interaction; Bounce rate from CTA destination

**Requirements:**
- Implement the treatment so it is correctly gated by LaunchDarkly flag `eh-desktop-nav-shop-all-destination-desktop` (read via the project’s existing LaunchDarkly client / hook pattern).
- Keep TypeScript and ESLint clean; match existing file structure and naming.
- If the repo uses Playwright (or another E2E runner), add or update tests so CI can prove the treatment behavior when the LaunchDarkly flag is **on** in the test environment (or document a test-only flag override if the team uses that pattern).
- If CI failed, read the GitHub Actions logs for this PR, fix the root cause, and push commits to the same branch.
```

_Generated by Experiment Hunter. Safe to delete after merge if you prefer not to keep it in the repo._
