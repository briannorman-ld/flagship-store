# Experiment Hunter — developer handoff

This file was generated automatically when **Test Idea** opened this pull request. If GitHub Actions or Playwright fails, share the sections below with your team or paste the prompt into your coding agent.

## Links

- **Flag (LaunchDarkly):** `eh-desk-category-dedup-desktop`
- **Experiment (LaunchDarkly):** `eh-desk-category-dedup-desktop-exp`

## Summary

### Title
Remove Duplicate Golf Category Link

### Hypothesis
If the duplicate Golf Flags link is replaced with a unique category (Country Flags), then category click distribution will be more even and total category tile click-through rate will rise, because each tile offers a distinct destination and the section feels more comprehensive.

### Arms
- **Control:** Eight category tiles where Golf appears twice — once as a gradient card and once as a bordered text link.
- **Treatment:** Eight unique category tiles: gradient cards for American, Nautical, State, Golf; text links for Country, Military, Historical, Garden & Yard.

### Metrics
Category tile click-through rate (eh-suggested-category_tile_click) — Category tile click-through rate; Category page views per session: Average number of PLP views per session originating from the homepage (flagship-store-plp-view). Indicates whether the unique tile drives incremental category browsing.; Pages per session: Average pages viewed per session for homepage visitors. A proxy for overall engagement lift.; FullStory) on both variants to visually confirm click distribution changes across the category grid.; category page views per session; pages per session

## Copy-paste prompt (for your dev / AI assistant)

```text
You are helping fix or finish a code change for a storefront repo opened by Experiment Hunter (A/B test wiring).

**LaunchDarkly flag key:** `eh-desk-category-dedup-desktop`
**LaunchDarkly experiment key:** `eh-desk-category-dedup-desktop-exp`

**Experiment title:** Remove Duplicate Golf Category Link
**Hypothesis:** If the duplicate Golf Flags link is replaced with a unique category (Country Flags), then category click distribution will be more even and total category tile click-through rate will rise, because each tile offers a distinct destination and the section feels more comprehensive.

**Control (current):** Eight category tiles where Golf appears twice — once as a gradient card and once as a bordered text link.
**Treatment (variant):** Eight unique category tiles: gradient cards for American, Nautical, State, Golf; text links for Country, Military, Historical, Garden & Yard.

**Metrics to preserve or instrument:** Category tile click-through rate (eh-suggested-category_tile_click) — Category tile click-through rate; Category page views per session: Average number of PLP views per session originating from the homepage (flagship-store-plp-view). Indicates whether the unique tile drives incremental category browsing.; Pages per session: Average pages viewed per session for homepage visitors. A proxy for overall engagement lift.; FullStory) on both variants to visually confirm click distribution changes across the category grid.; category page views per session; pages per session

**Requirements:**
- Implement the treatment so it is correctly gated by LaunchDarkly flag `eh-desk-category-dedup-desktop` (read via the project’s existing LaunchDarkly client / hook pattern).
- Keep TypeScript and ESLint clean; match existing file structure and naming.
- If the repo uses Playwright (or another E2E runner), add or update tests so CI can prove the treatment behavior when the LaunchDarkly flag is **on** in the test environment (or document a test-only flag override if the team uses that pattern).
- If CI failed, read the GitHub Actions logs for this PR, fix the root cause, and push commits to the same branch.
```

_Generated by Experiment Hunter. Safe to delete after merge if you prefer not to keep it in the repo._
