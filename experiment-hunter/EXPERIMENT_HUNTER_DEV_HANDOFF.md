# Experiment Hunter — developer handoff

This file was generated automatically when **Test Idea** opened this pull request. If GitHub Actions or Playwright fails, share the sections below with your team or paste the prompt into your coding agent.

## Links

- **Flag (LaunchDarkly):** `eh-desktop-visible-card-atc-desktop`
- **Experiment (LaunchDarkly):** `eh-desktop-visible-card-atc-desktop-exp`

## Summary

### Title
Show product-card add-to-cart buttons by default

### Hypothesis
If we change homepage product cards so add-to-cart buttons show without hover, then FlagShip — Add to cart will improve, because the purchase action becomes visible during product evaluation instead of being hidden behind a hover state.

### Arms
- **Control:** Homepage product cards show product details, ratings, and prices, with the add-to-cart button only becoming visible on hover.
- **Treatment:** Homepage product cards always show a full-width add-to-cart button beneath the price, while retaining the hover color change as a secondary state.

### Metrics
FlagShip — Add to cart; FlagShip — PDP view; FlagShip — Cart view; Homepage Best Seller Product Click

## Copy-paste prompt (for your dev / AI assistant)

```text
You are helping fix or finish a code change for a storefront repo opened by Experiment Hunter (A/B test wiring).

**LaunchDarkly flag key:** `eh-desktop-visible-card-atc-desktop`
**LaunchDarkly experiment key:** `eh-desktop-visible-card-atc-desktop-exp`

**Experiment title:** Show product-card add-to-cart buttons by default
**Hypothesis:** If we change homepage product cards so add-to-cart buttons show without hover, then FlagShip — Add to cart will improve, because the purchase action becomes visible during product evaluation instead of being hidden behind a hover state.

**Control (current):** Homepage product cards show product details, ratings, and prices, with the add-to-cart button only becoming visible on hover.
**Treatment (variant):** Homepage product cards always show a full-width add-to-cart button beneath the price, while retaining the hover color change as a secondary state.

**Metrics to preserve or instrument:** FlagShip — Add to cart; FlagShip — PDP view; FlagShip — Cart view; Homepage Best Seller Product Click

**Requirements:**
- Implement the treatment so it is correctly gated by LaunchDarkly flag `eh-desktop-visible-card-atc-desktop` (read via the project’s existing LaunchDarkly client / hook pattern).
- Keep TypeScript and ESLint clean; match existing file structure and naming.
- If the repo uses Playwright (or another E2E runner), add or update tests so CI can prove the treatment behavior when the LaunchDarkly flag is **on** in the test environment (or document a test-only flag override if the team uses that pattern).
- If CI failed, read the GitHub Actions logs for this PR, fix the root cause, and push commits to the same branch.
```

_Generated by Experiment Hunter. Safe to delete after merge if you prefer not to keep it in the repo._
