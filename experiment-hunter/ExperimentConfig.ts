export const FLAG_KEY = 'eh-desktop-cta-shipping-inline-desktop';

/**
 * This module exports the LaunchDarkly flag key for the experiment.
 * To use the flag, import `FLAG_KEY` and call `useLDClient` from the
 * `launchdarkly-react-client-sdk` to get the flag variation.
 */